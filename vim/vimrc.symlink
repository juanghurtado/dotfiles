set nocompatible

let mapleader="\<Space>"
let g:mapleader="\<Space>"

syntax on

for file in ["vundle.vim", "options.vim", "mappings.vim"]
	if filereadable(expand("~/.vim/". file))
		exe "source ~/.vim/". file
	endif
endfor